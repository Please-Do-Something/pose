# 프로젝트 구조 — 자세 교정 모니터

웹캠으로 실시간 자세(거북목(오래 고개 숙임 포함), 어깨 기울어짐, 화면에 너무 가까이 붙음)를 감지해 알려주고, 기록을 대시보드로 보여주는 PyQt5 데스크톱 앱.

## 디렉터리 구조

```
pose/
├── main.py              # [Layer 1] PyQt5 GUI 메인 윈도우 & 대시보드
├── camera_worker.py     # [Layer 2] 웹캠 캡처 & MediaPipe 랜드마크 추출 (QThread)
├── posture_logic.py     # [Layer 3] 한 프레임의 지표/점수 계산, 기준 자세·자동 임계 (순수 로직)
├── posture_tracker.py   # [Layer 3] 평활 → 항목별 확정/복귀 → 시간 집계 → 알림 판단 (순수 로직, 시각을 인자로 받음)
├── recorder.py           # 튜닝용 랜드마크 녹화(JSONL) 쓰기/읽기
├── replay.py             # 녹화 파일을 앱과 같은 판정 코드로 재생/채점하는 CLI
├── tests/test_posture.py # 합성 랜드마크 단위 테스트 (python -m unittest discover tests)
├── recordings/           # 녹화 파일 (런타임 생성, git 제외)
├── db_manager.py         # [Layer 4] SQLite 저장 & 집계
├── posture.db             # db_manager.py가 생성하는 SQLite DB (런타임 생성, 최초 실행 전엔 없음)
├── baseline.json         # 기준 자세 저장 파일 (캘리브레이션 시 생성, git 제외)
├── requirements.txt       # 의존성 목록
├── architect.txt           # 원본 설계 스펙 (영문, AI 코드 생성용 프롬프트 문서)
├── README.md               # 프로젝트 개요 & 실행법
└── venv/                    # 가상환경 (Python 3.11.9)
```

## 아키텍처: 4-Layer 구조

```
┌─────────────────────────────────────────────────────────┐
│ main.py (Layer 1 — GUI)                                   │
│  MainWindow(QMainWindow)                                  │
│   - 좌측: 웹캠 프리뷰 + 상태 텍스트 + 캘리브레이션 버튼        │
│           + 디버그/녹화 체크박스 (녹화 중 숫자키 0~5로 라벨)    │
│   - 우측: 대시보드(시간 기반 점수 / 항목별 횟수 / 시간대별 이상 자세 분) │
│   - QTimer로 30초마다 대시보드 갱신                          │
└───────────────┬─────────────────────────────────────────┘
                │ pyqtSignal: frame_ready(QImage), status_updated(dict), calibration_finished(bool, str)
┌───────────────▼─────────────────────────────────────────┐
│ camera_worker.py (Layer 2 — QThread)                       │
│  CameraWorker                                              │
│   - cv2.VideoCapture(0) 캡처, 모델 입력만 감마 보정            │
│   - mediapipe pose → 귀(7,8)/어깨(11,12) 좌표·신뢰도             │
│   - 캘리브레이션 진행(카운트다운/샘플 수집), baseline.json 저장   │
│   - PostureTracker 결과를 DB(이벤트/시간)·토스트 알림·화면에 연결  │
│   - 녹화가 켜져 있으면 프레임마다 recorder로 기록                 │
└───────────────┬─────────────────────────────────────────┘
                │ 함수 호출
┌───────────────▼─────────────────────────────────────────┐
│ posture_tracker.py (Layer 3 — 시간축 판정, 순수 로직)            │
│  measurement_from_points / Smoother / IssueState / PostureTracker │
│   - process(now, m, brightness) → {hold, sm, eval, events, notify} │
│   - pop_time() → 시간대별 판정 시간·이상 자세 시간                 │
│ posture_logic.py (Layer 3 — 프레임 지표, 순수 로직)              │
│   - neck_ratio / shoulder_tilt / shoulder_width                 │
│   - evaluate_posture → 항목별 "임계 대비 점수"                     │
│   - auto_thresholds / baseline_from_samples                    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ db_manager.py (Layer 4 — 저장/집계)                          │
│  DBManager (SQLite: posture.db, 연결 사용은 잠금으로 직렬화)      │
│   - start_event / end_event: 이상 자세 한 번 = 한 행            │
│   - add_time: 시간대별 판정 시간·이상 자세 시간 누적               │
│   - get_daily_summary(date) → 점수/항목별 횟수/시간대별 이상 분    │
└─────────────────────────────────────────────────────────┘

replay.py ──(같은 PostureTracker로)── recordings/*.jsonl 재생·채점 (카메라/GUI 불필요)
```

`main.py`가 `DBManager` 인스턴스를 만들어 `CameraWorker`에 주입하므로, DB 기록은 워커 스레드 안에서 이뤄지고 대시보드 조회는 메인 스레드에서 한다(연결은 잠금으로 공유).

## 판정 로직 (posture_logic.py + posture_tracker.py)

캘리브레이션(`기준 자세 설정` 버튼) 이전/이후로 판정 기준이 다르다:

| 지표 | 캘리브레이션 전 | 캘리브레이션 후 |
|---|---|---|
| 거북목 | 판정 안 함 | 목 길이비가 기준 대비 15% 이상 감소 (아래 설명). 오래 고개를 숙이고 있어도 여기에 잡힌다. 캘리브레이션 때 귀가 안 보였으면 판정 안 함 |
| 구부정(화면에 가까움) | 판정 안 함 | 어깨너비가 기준 대비 1.15배 이상 |
| 어깨 기울어짐 | 판정 안 함 | 기준 기울기 대비 변화량 > 0.07, 약 4도 (웹캠 기울어짐/평소 체형 보정). 0.05였으나 모니터 빛 변화와 마우스 쪽 어깨 올라감으로 바른 자세의 17%가 오탐돼 올림 |

캘리브레이션 전에는 판정·DB 기록·알림을 모두 하지 않는다.

**점수 방식:** 각 항목은 프레임마다 "임계 대비 점수"(1.0 = 임계에 닿음, 0 = 기준 자세와 같음)로 계산되고, 확정/복귀는 항목마다 따로 판단한다(`IssueState`).
- 확정: 최근 `SUSTAIN_SECONDS`(2초) 동안 프레임의 `CONFIRM_FRACTION`(80%) 이상이 점수 1.0 이상. 한두 프레임이 임계 아래로 튀어도 유지 시간이 0으로 돌아가지 않는다. 대신 실제로는 유지 시간의 80%만 넘어도 확정된다.
- 복귀: 확정 뒤에는 점수가 `EXIT_SCORE`(0.7) 아래인 프레임이 최근 `RECOVER_SECONDS`(1초)의 80% 이상일 때 복귀(히스테리시스). 판정할 수 없는 프레임(예: 귀가 안 보임)은 복귀 쪽으로 친다.
- 판정 공백이 `MAX_GAP`(1초)보다 길면 유지 이력을 처음부터 다시 쌓는다. 보류가 `ABSENCE_END_SECONDS`(5초) 이어지면(자리 비움 등) 확정된 이상 자세를 모두 끝낸다.
- 예전에는 정상/이상 하나의 상태만 있어서, 거북목 1.2초 + 기울어짐 1.2초처럼 서로 다른 문제가 이어져도 확정되는 문제가 있었다.

**고개 숙임은 거북목으로 본다 (구분 기능을 넣었다가 뺀 이력):** 목 길이비는 키보드를 내려다봐도 줄어든다. 짧게 내려다보는 건 2초 유지 조건이 거르고(실측: 평소처럼 일하는 구간의 거북목 판정 2%), 계속 숙이고 있으면 거북목으로 잡힌다. 오래 숙이는 것도 목에 부담이 되는 자세라 같은 알림을 준다.
- 코-귀 높이차(귀거리 기준)로 숙임을 가려 거북목에서 빼는 방식을 시도했다. 2026-09-24 실측 녹화 2개(약 8분)에서 이 정면 카메라로는 고개를 내밀 때도 코가 0.10~0.17 내려가, 가볍게 숙인 자세(0.17)와 구분되지 않았다. 크게 숙여도 귀가 같이 내려가 0.15밖에 차이 나지 않았다.
- 숙임 예외를 두면 거북목 탐지율이 7%까지 떨어졌고(예외 기준 0.105), 예외를 거의 없애야(0.30) 96%가 됐다. 그 상태에서도 따로 둔 "고개 숙임 지속"(30초) 판정은 한 번도 확정되지 않아 기능 자체를 뺐다.

**알림:** 확정될 때 알리되, 같은 항목은 `NOTIFY_COOLDOWN`(60초) 안에 다시 확정되면 알리지 않는다. 확정 상태가 이어지면 `RENOTIFY_SECONDS`(5분)마다 다시 알린다.

캘리브레이션 흐름: 버튼 클릭 → 3초 카운트다운(`CALIB_COUNTDOWN_SECONDS`, 이 동안 판정과 같은 평활을 미리 돌림) → 3초간(`CALIB_SAMPLE_SECONDS`) 어깨가 보이는 프레임의 평활값을 모아 중앙값으로 기준 확정(`posture_logic.baseline_from_samples`). 귀가 샘플의 절반 이상에서 보였을 때만 거북목 기준을 잡는다. 측정을 시작하면 진행 중인 이상 자세는 끝난 것으로 기록한다.

**자동 임계값:** 측정 3초 동안 모은 평활값의 흔들림(표준편차)으로 이 카메라/환경에 맞는 임계를 정한다(`posture_logic.auto_thresholds`). 각 임계는 "기본값과 흔들림×3(`NOISE_MULTIPLIER`) 중 큰 값"이며 상한(거북목 30%, 어깨너비 x1.30, 기울기 0.12)이 있다. 상한에 걸리면 "판정이 둔할 수 있으니 조명을 밝게 해 보라"는 안내를 띄운다. 자동 임계는 `baseline.json`에 함께 저장되고, 디버그 화면에서 기본값보다 넓어진 임계에는 "자동"이 표시된다.
- 한계: 3초 안에 드러나는 흔들림만 잰다. 주기가 10초 이상인 느린 출렁임은 과소평가되고, 2초 유지 조건으로도 걸러지지 않아 오탐이 날 수 있다(시뮬레이션: ±0.03, 주기 10초에서 고정/자동 모두 2분에 약 4건). 짧은 흔들림은 평활과 2초 유지 조건이 대부분 흡수한다. 진행 상황과 결과("기준 자세가 설정되었습니다" / 실패 안내)는 영상 오버레이·상태바로 표시되고, 유효 샘플이 10개 미만이면 실패로 처리하며 이전 기준을 유지한다. 측정은 워커 스레드 안에서 이뤄지고 GUI는 요청만 보낸다.

판정 전에 최근 5프레임 중앙값(`MEDIAN_WINDOW`)으로 튀는 값을 거른 뒤 지수평활(`SMOOTH_TAU`: 어깨 1.0초, 귀 0.5초)해 프레임별 노이즈로 판정이 깜빡이지 않게 한다. 어깨는 옷/배경과 경계가 흐려 특히 많이 흔들리고, 세 판정 모두 어깨 값을 쓰므로 더 강하게 평활한다. 화면 평균 밝기가 `MIN_BRIGHTNESS`(3) 미만(카메라 가림/완전 암흑)이거나 어깨 신뢰도가 낮거나 사람이 인식되지 않으면 그 프레임은 판정하지 않고, 1초 넘게 이어지면 "판정 보류: 사유"를 표시한다. 보류 구간은 점수 계산의 판정 시간에서도 빠진다.

**조명 영향과 거북목 판정 방식:** 화면 좌표(x,y) 기반의 **목 길이비** 하나로 판정한다.
- 목 길이비 = (어깨 중점 y − 귀 중점 y) / 양 귀 사이 거리. 고개가 앞으로 빠지면 귀가 내려와 분자가 줄고, 머리가 카메라에 가까워져 분모가 커지므로 두 효과 모두 값을 줄인다. 기준 대비 15% 이상 감소(`NECK_DROP_RATIO`)하면 거북목.
- 카메라와의 거리가 바뀌면 분자·분모가 같이 변해 비율이 유지되고, 고개를 좌우로 돌려도 세로 간격은 그대로다. 고개를 숙여도 값이 줄어 계속 숙이고 있으면 거북목으로 잡힌다(2초 유지 조건으로 짧은 숙임은 걸러짐).
- 변경 이력(실측 근거):
  1. mediapipe z(깊이) → 같은 자세에서 조명에 따라 판정 여유만큼 달라짐(밝기 7↔27에서 -0.02~+0.04).
  2. 어깨너비로 나눈 비율 → 어두우면 어깨가 약 7% 넓게 잡혀 비율이 같이 줄어듦(밝기 32→8에서 세로간격/어깨너비 0.60→0.56, 귀거리/어깨너비 0.425→0.40).
  3. 귀 사이 거리로 나눈 비율(현재) → 같은 실측으로 1.41→1.40, 조명 영향이 거의 상쇄됨.
- 어두운 화면에서 인식 자체가 흔들리지 않도록 모델 입력만 감마 보정해 평균 밝기를 `TARGET_BRIGHTNESS`(110) 근처로 맞춘다(화면 표시는 원본, 항상 켜짐).

**앉은 거리 변화 자동 보정:** 의자를 당기거나 빼면 원근 때문에 바른 자세여도 목 길이비가 바뀐다. 귀와 어깨의 깊이 차와 카메라 높이 탓이고, 카메라 모델로 계산하면 70→50cm에서 귀 기준 약 −5%다(카메라가 높고 어깨가 뒤에 있으면 더 커짐). 원근 계수는 카메라와 체형마다 달라 미리 알 수 없으므로, 자리를 옮기는 **순간**을 잡아 실제로 생긴 차이만큼 거북목 기준을 옮긴다(`PostureTracker._track_seat`). 카메라별 조정도, 사용자의 재설정도 필요 없다.
- 감지: 평활된 어깨너비가 3초(`SEAT_MOVE_WINDOW`) 안에 10%(`SEAT_MOVE_WIDTH_CHANGE`) 이상 변하면 자리 이동으로 본다(약 7cm 이상). 상체만 빠르게 숙이는 동작(모델상 어깨너비 +7%)이나 천천히 무너지는 자세는 걸리지 않는다.
- 이동 중: 거북목 판정만 멈춘다. 모니터 근접과 어깨 기울어짐은 계속 판정한다(가까이 앉은 것 자체는 모니터 근접으로 잡혀야 함).
- 자리 잡음: 어깨너비가 1.5초 동안 2% 안에서 안정되면, 이동 **직전**과 **직후**의 목 길이비 비율만큼 거북목 기준(`neck`)을 옮긴다. 어깨너비·기울기 기준은 그대로 둔다.
- 안전장치: 이동 직전이 정상 자세였을 때만 적용한다(거북목 점수 0.7 미만, 확정 아님). 한 번에 ±20%(`MAX_SEAT_SHIFT`), 처음 잡은 기준에서 누적 ±30%(`MAX_TOTAL_SEAT_SHIFT`)를 넘으면 자세도 바뀐 것으로 보고 옮기지 않는다. 10초 안에 자리를 잡지 않으면 취소한다.
- 옮긴 기준은 `baseline.json`에 저장하고, 처음 잡은 기준은 `neck_calibrated`로 함께 보관한다. 기준 자세를 다시 잡으면 초기화된다. 녹화에는 기준 줄을 쓰지 않는다(재생 때 트래커가 같은 보정을 다시 한다).
- 한계: 20초에 걸쳐 천천히 당겨 앉는 것처럼 느린 이동은 감지하지 못해 예전처럼 거북목 오탐이 날 수 있다. 자리를 옮기면서 동시에 자세가 무너지면 한도 안에서는 그 몫까지 기준에 흡수된다. 어깨를 안으로 말거나 조명이 어두워 어깨너비가 크게 바뀌는 경우도 이동으로 오인할 수 있다.

**기준 자세 저장:** 캘리브레이션 결과는 `baseline.json`에 저장되고 다음 실행 때 자동으로 불러온다. 카메라 위치를 바꿨을 때만 다시 설정하면 된다. 지표 계산식이 바뀌면 `BASELINE_VERSION`을 올려 예전 파일은 무시하고 재설정을 안내한다. 저장된 임계가 현재 기본값보다 좁으면 불러올 때 기본값으로 넓힌다(`merge_thresholds`). 그래서 기본값을 올려도 재설정 없이 반영된다. 예전 파일에 남아 있는 `pitch`/`nod`(고개 숙임 기능) 값은 무시한다.

**디버그 모드:** GUI의 "디버그 정보 표시" 체크박스를 켜면 영상 좌하단에 판정 수치(목 길이비와 기준·감소율·임계, 어깨너비 비율, 어깨기울기 차이, 귀·어깨 신뢰도, 화면 밝기, FPS)가 표시된다. 항목마다 확정 진행 상황(`[85% 1.2/2s]` = 최근 1.2초 중 85%가 임계 이상, `[확정]`)이 붙는다. 임계값을 조정할 때 참고용이다.

## 튜닝: 녹화와 재생

1. GUI에서 "판정 데이터 녹화 (튜닝용)"를 켠다. `recordings/YYYYmmdd_HHMMSS.jsonl`에 프레임마다 귀·어깨 좌표/신뢰도, 밝기, 현재 라벨이 기록된다(영상은 저장하지 않음). 기준 자세를 다시 잡으면 그 시점의 기준도 기록된다.
2. 녹화 중 숫자키로 지금 자세의 정답 라벨을 붙인다: `1` 바른 자세, `2` 거북목, `3` 고개 숙임(판정은 거북목으로 나오는 게 정상), `4` 어깨 기울임, `5` 화면에 가까이, `0` 라벨 없음.
3. `python replay.py recordings/<파일>.jsonl`로 앱과 같은 `PostureTracker`에 다시 돌려 항목별 확정 횟수/시간과, 라벨별로 순간/확정 판정 비율(오탐, 탐지율)을 본다.
4. `--set neck_drop=0.12 --set tilt=0.08`처럼 임계를 바꿔 같은 녹화로 비교하고, `--csv`로 프레임별 수치를 뽑아 그래프로 볼 수 있다.

## 데이터베이스 스키마 (posture.db)

```sql
-- 이상 자세 한 번 = 한 행. 복귀하면 end_ts가 채워진다 (비정상 종료 시 NULL로 남음)
CREATE TABLE posture_events (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    issue TEXT NOT NULL,        -- turtle / lean / tilt (예전 nod 행은 집계에서 무시)
    start_ts DATETIME NOT NULL,
    end_ts DATETIME,
    metric REAL                 -- 확정 시점 지표 (목 길이비 / 어깨너비 / 기울기 / 고개 각도)
);

-- 시간대별 판정 시간과 그중 이상 자세였던 시간(초). 워커가 10초마다, 종료 시 누적한다.
CREATE TABLE posture_time (
    date TEXT, hour INTEGER,
    monitored_sec REAL, bad_sec REAL,
    turtle_sec REAL, lean_sec REAL, tilt_sec REAL,   -- 예전 DB에는 nod_sec 열이 남아 있을 수 있음 (항상 0)
    PRIMARY KEY (date, hour)
);

-- 예전 방식(확정 순간 1건씩)의 기록. 과거 데이터 보존용으로 남아 있고 더 이상 쓰지 않는다.
CREATE TABLE posture_logs (id, timestamp, is_turtle, is_imbalanced, neck_angle);
```

- 점수 = `100 × (1 − 이상 자세 시간 / 판정 시간)`. 판정 시간이 1분 미만이면 점수를 내지 않는다. 예전 `100 − 이벤트 수×2`는 3시간 내내 거북목이어도 98점이 나왔다.
- 대시보드 차트는 시간대별 이상 자세 시간(분)을 보여준다.

## 의존성 (requirements.txt)

| 패키지 | 용도 |
|---|---|
| opencv-python | 웹캠 캡처, 이미지 처리 |
| mediapipe | 자세(Pose) 랜드마크 추출 AI 모델 |
| PyQt5 | GUI 프레임워크 |
| pyqtgraph | 대시보드 시간대별 분포 바 차트 |
| pyinstaller | .exe 빌드 |
| numpy | 벡터/각도 계산 |
| Pillow | 한글 텍스트 오버레이 (cv2.putText 한글 미지원 우회) |
| winotify | Windows 토스트 알림 (미설치 시 알림만 비활성화, 앱은 정상 동작) |

가상환경: `venv/` (Python 3.11.9)

## 테스트

```bash
python -m unittest discover tests
```

## 실행

```bash
.\venv\Scripts\Activate.ps1
python main.py
```

최초 실행 후 카메라 앞에 바른 자세로 앉아 **'기준 자세 설정'** 버튼을 눌러 캘리브레이션해야 판정이 정확해진다.

## 빌드 (.exe)

```bash
pyinstaller -w -F main.py
```
